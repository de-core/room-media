'use strict';

/* Filters */
var app_f = angular.module('roomFilters', [])

	// app_f.filter('hasVideo', function() {
	// 	return function (items) {
	//     var filtered = [];
	//     for (var i = 0; i < items.length; i++) {
	//       var item = items[i];
	//       console.log(item)
	//       if (item.videourl) {
	//         filtered.push(item);
	//         console.log('pushed')
	//       }
	//     }
	//     return filtered;
 //  	}
	// });